/* 
管理用户信息数据的 reducer 函数
*/

const initUser = 1;
export default function user(state=initUser,action){

  switch (action.type) {
    /* case value:
      
      break; */
  
    default:
      return state;
  }

}